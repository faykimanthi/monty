#include "monty.h"
/**
 * f_stack - this sets the format of the data to a stack (LIFO). This is the default behavior of the program
 * @head: this represents the head of the stack
 * @counter: represents the line_number
 * Return: no
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
